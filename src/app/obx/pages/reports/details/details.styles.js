import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles((_theme) => ({
  reportView: {
    maxWidth: '860px',
    width: '100%',
    margin: '0 auto',
    padding: '16px',
  },
}));
