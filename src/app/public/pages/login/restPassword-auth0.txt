<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
  <title>Change your password</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
 
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />

  <style type="text/css">
    body,html{
      padding:0;
      margin:0;
      font-family: 'Inter', sans-serif;
    }
    

    
    .table{
      font-family: 'Inter', sans-serif;
      display:table;
      position:absolute;
      height:100%;
      width:100%;
      {% unless tenant.colors.page_background %}
        background:linear-gradient(rgba(255,255,255,.3),rgba(255,255,255,0));
      {% endunless %}
      background-color: {{tenant.colors.page_background | default: '#e8ebef'}};
    }
    .cell{
      display:table-cell;
      vertical-align:middle;
      background:white;
    }
    
    .auth0-lock.auth0-lock{
      font-family: 'Inter', sans-serif !important;
    }
    
    #change-password-widget-container{
      font-family: 'Inter', sans-serif;
    	display:flex;
      flex-direction:column;
      justify-content:center;
      align-items:center;
      min-height:100vh;
    }
    
    .auth0-lock.auth0-lock.auth0-lock-opened-in-frame{
     	border-radius: 16px;
			border: 1px solid #D0CFD2;
    	max-width: 410px;
      width:100% !important;
    	padding: 50px 25px;
    	text-align: center;
    	z-index: 1;
    }
    
    .auth0-lock.auth0-lock .auth0-lock-header-logo{
      height:36px !important;
      display:block !important;
      margin:0 auto !important;
    }
    
    .auth0-lock.auth0-lock .auth0-lock-widget{
    width:100% !important;
    }
    .auth0-lock.auth0-lock .auth0-lock-content{
   padding:0px !important;
     
    }
    .auth0-lock.auth0-lock .auth0-lock-name{ 
     	color:  #000;
			text-align: center;
			font-size: 24px;
			font-style: normal;
			font-weight: 700;
			line-height: 32px;
    }
    
    .pulse {
    -webkit-animation-name: none !important;
    animation-name: none !important;
}
    
    .auth0-lock.auth0-lock .auth0-lock-header-welcome{
     	display:flex;
      flex-direction:column;
      gap:36px;
    }
    .auth0-lock.auth0-lock .auth0-lock-header-bg{
    display:none;
    }
    .auth0-lock.auth0-lock .auth0-lock-header{
      padding:0px 0px 14px 0px !important;
    
    }
    /* Added by Wasi */
    .auth0-lock-submit{
    	height: 50px !important;
      color: white !important;
    }
    /* Added by Wasi */
    .auth0-lock.auth0-lock .auth0-lock-submit{
      border-radius:8px !important;
      padding:10px 16px !important;
    }
    
    .auth0-lock.auth0-lock .auth0-lock-input-wrap{
      border:0 !important;
      padding-left:0 !important;
      background:transparent !important;
    }
    
    .auth0-lock.auth0-lock .auth0-lock-input-wrap span > svg{
     display:none !important;
    }
    
    
    
    .auth0-lock.auth0-lock .auth0-lock-input-wrap .auth0-lock-input:focus {
  border: 1px solid #146DFF !important;
        box-shadow: 0px 0px 0px 4px rgba(14, 165, 233, 0.1) !important;
}
  
    .auth0-lock.auth0-lock .auth0-lock-error .auth0-lock-input-wrap .auth0-lock-input:focus{
  border: 1px solid #DF372B !important;
  transition: border-color 0.3s ease; /* Add a smooth transition for the border color */
}


    .auth0-lock.auth0-lock .auth0-lock-input-wrap .auth0-lock-input{
    display: block !important;
    padding: 10px 14px 10px 14px !important;
    border-radius: 8px !important;
    border: 1px solid #D0CFD2 !important;
    background: #FFF !important;
    color: #262527 !important;
    font-family: inherit !important;
    font-size: 16px !important;
    font-style: normal !important;
    font-weight: 400 !important;
    line-height: 24px !important;
    height: 40px !important;
    width: calc(100% - 8px) !important;
    margin: 0 auto !important;
    }
    
    
        .auth0-lock.auth0-lock .auth0-lock-input-wrap .auth0-lock-input:focus:hover{
 border: 1px solid #146DFF !important;
  transition: border-color 0.3s ease; /* Add a smooth transition for the border color */
}
    
    	.auth0-lock.auth0-lock .auth0-lock-error .auth0-lock-input-wrap .auth0-lock-input:focus {
  border: 1px solid #DF372B !important;
  box-shadow: 0px 0px 0px 4px #fee4e2, 0px 1px 2px 0px rgba(16, 24, 40, 0.05) !important;
}
   

    .auth0-lock.auth0-lock .auth0-lock-input-wrap .auth0-lock-input:hover{
       border-color: #AEAEB2 !important;
    }
    
    .auth0-lock.auth0-lock .auth0-lock-input-wrap .auth0-lock-input::placeholder{
    	color: #6A6A70 !important;
    }
    
    /*
    .auth0-lock.auth0-lock .auth0-lock-cred-pane{
      display:flex;
      flex-direction:column;
      gap:36px;
    }
    */
    .auth0-lock.auth0-lock .auth0-lock-form p{
      font-weight: 400;
    font-size: 14px !important;
    letter-spacing: normal;
    line-height: 20px !important;
      color:#86868B !important;
      margin-top: 4px !important;
      margin-bottom: 36px !important;
    }
    
    
        .auth0-lock.auth0-lock .auth0-lock-submit{
        display:flex !important;
        justify-content:center !important;
        align-items:center !important;
        padding:10px 14px !important;
        height:40px !important;
        border-radius: 8px !important;
				border: 1px solid #146DFF !important;
				background:  #146DFF !important;
        color:  #FFF !important;
				font-family: inherit!important;
				font-size: 14px !important;
				font-style: normal !important;
				font-weight: 500 !important;
				line-height: 20px !important;
        width:calc(100% - 8px) !important;
        margin: 0 auto !important;
        margin-top:36px !important;
        cursor:pointer !important;
        transition: background-color 250ms cubic-bezier(0.4, 0, 0.2, 1) 0ms,box-shadow 250ms cubic-bezier(0.4, 0, 0.2, 1) 0ms,border-color 250ms cubic-bezier(0.4, 0, 0.2, 1) 0ms,color 250ms cubic-bezier(0.4, 0, 0.2, 1) !important;
    		}
    
     .auth0-lock.auth0-lock .auth0-lock-submit:hover{
      background-color:#0059FF !important;
    }
    
     .auth0-lock.auth0-lock .auth0-lock-submit:active{
    	box-shadow: 0px 0px 0px 4px #E5F6FF, 0px 1px 2px 0px rgba(16, 24, 40, 0.05) !important;
		}
    
     .auth0-lock.auth0-lock .auth0-lock-submit:disabled{
      border: 1px solid  #A9DEFF !important;
			background:  #A9DEFF !important;
     	cursor: not-allowed !important;
    }
    
    .back-login{
      color:#5B5B5F;
			font-size: 14px;
			font-style: normal;
			font-weight: 500;
			line-height: 20px;
      padding-top:26px;
      text-decoration:none;
      display:block;
    }
    
   
    
    @media (min-width: 481px){

.auth0-lock.auth0-lock .auth0-lock-input-block {
    margin-bottom: 16px !important;
}
    }
    
    

    .content{
      background:white;
    }
  </style>


</head>
<body>
  <div class="table">
    <div class="cell">
      <div class="content">
        <!-- WIDGET -->
        <div id="change-password-widget-container"></div>
        <!-- END WIDGET -->
      </div>
    </div>
  </div>

  <script src="https://cdn.auth0.com/js/change-password-1.5.min.js"></script>

  <script>
    new Auth0ChangePassword({
      container:         "change-password-widget-container",                // required
      email:             "{{email | escape}}",                              // DO NOT CHANGE THIS
      csrf_token:        "{{csrf_token}}",                                  // DO NOT CHANGE THIS
      ticket:            "{{ticket}}",                                      // DO NOT CHANGE THIS
      password_policy:   "{{password_policy}}",                             // DO NOT CHANGE THIS
      password_complexity_options:  {{password_complexity_options}},        // DO NOT CHANGE THIS
  		theme: {
        icon: "{{tenant.picture_url | default: 'https://signalassets.blob.core.windows.net/signal/assets/signalLogo.svg'}}",
        primaryColor: "{{tenant.colors.primary | default: '#146DFF'}}"
      },
      
 
      dict: {
        // passwordPlaceholder: "your new password",
        // passwordConfirmationPlaceholder: "confirm your new password",
        // passwordConfirmationMatchError: "Please ensure the password and the confirmation are the same.",
        // passwordStrength: {
        //   containsAtLeast: "Contain at least %d of the following %d types of characters:",
        //   identicalChars: "No more than %d identical characters in a row (e.g., "%s" not allowed)",
        //   nonEmpty: "Non-empty password required",
        //   numbers: "Numbers (i.e. 0-9)",
        //   lengthAtLeast: "At least %d characters in length",
        //   lowerCase: "Lower case letters (a-z)",
        //   shouldContain: "Should contain:",
        //   specialCharacters: "Special characters (e.g. !@#$%^&*)",
        //   upperCase: "Upper case letters (A-Z)"
        // },
        // successMessage: "Your password has been reset successfully.",
        // configurationError: "An error ocurred. There appears to be a misconfiguration in the form.",
        // networkError: "The server cannot be reached, there is a problem with the network.",
        // timeoutError: "The server cannot be reached, please try again.",
        // serverError: "There was an error processing the password reset.",
        // headerText: "Enter a new password for<br />{email}",
        // title: "Change Password",
        // weakPasswordError: "Password is too weak."
        // passwordHistoryError: "Password has previously been used."
      }
    });
    
    
    var element = document.querySelector('.auth0-lock-name');

// Check if the element was found
if (element) {
  // Update the text
  element.textContent = 'Reset Password';
}
    
    // Access the <span> element by its class name
var spanElement = document.querySelector(".auth0-lock-form span");

// Check if the <span> element exists
if (spanElement) {
    // Remove the <br> tag from the span's innerHTML
    spanElement.innerHTML = spanElement.innerHTML.replace(/<br>/g, ' ');

    // You can also directly set the text content of the span
    spanElement.textContent = "Enter new password for your account so you can login and access features.";
}
    
    // Access the input elements by their class names
var inputElements = document.querySelectorAll(".auth0-lock-input");

// Check if the input elements exist and change their placeholders
if (inputElements.length >= 2) {
    // Change the placeholder text for the first input
    inputElements[0].placeholder = "Enter New Password";

    // Change the placeholder text for the second input
    inputElements[1].placeholder = "Confirm New Password";
}

    // Access the button element by its class name
var buttonElement = document.querySelector(".auth0-lock.auth0-lock .auth0-lock-submit");

// Check if the button element exists
if (buttonElement) {
    // Change the button text
    buttonElement.textContent = "";
    buttonElement.textContent = "Update Password";
 
}
    
    // Access the button element by its class name
var buttonElement = document.querySelector(".auth0-lock-submit");

// Check if the button element exists
if (buttonElement) {
    // Create a new anchor tag
    var anchorElement = document.createElement("a");

    // Set the anchor tag's attributes
    anchorElement.href = "https://example.com"; // Set the URL for the anchor
    anchorElement.textContent = "Back to Login"; // Set the anchor text

    // Add a class to the anchor tag
    anchorElement.classList.add("back-login"); // Add the class "back-login" to the anchor

    // Append the anchor tag below the button
    buttonElement.parentNode.appendChild(anchorElement);
}

    
    var parentDiv = document.querySelector('.auth0-lock-cred-pane'); // replace with the actual class of your parent div

if (parentDiv) {
  
    var firstSpan = parentDiv.querySelector('span');

   
    if (firstSpan) {
        // Remove the first span element
        firstSpan.parentNode.removeChild(firstSpan);
    }
}
    //From here added by Wasi
    // Get all elements with the class '.auth0-lock-submit span'
var buttonSvg = document.querySelectorAll('.auth0-lock-submit span');

// Loop through the selected elements and remove them
buttonSvg.forEach(function(element) {
  element.parentNode.removeChild(element);
});

 //Till here
  </script>
 
</body>
</html>
